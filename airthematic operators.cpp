#include<stdio.h>
int main()
{
	 char ch;
	 printf("enter the character:");
	 scanf("%d",&ch);
	 
	if((ch= '+'' && '-' && '*' && '/'))
	printf("arithematic operator verified",ch);
	else
		printf("arithematic operator not verified",ch);
		
		return 0;
	 
	 
}
